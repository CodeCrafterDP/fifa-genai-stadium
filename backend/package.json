{
  "name": "matchday-os-backend",
  "version": "1.0.0",
  "description": "Backend for MatchDay OS — shared-state storage API + Anthropic API proxy, also serves the frontend.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
